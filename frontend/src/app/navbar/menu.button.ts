export class MenuButton {
    url: string;
    name: string;
    icon: string;
    position: number;

    constructor(url: string, name: string, icon: string) {
        this.url = url;
        this.name = name;
        this.icon = icon;
    }
}
