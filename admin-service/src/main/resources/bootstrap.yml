spring:
  application:
    name: admin-service
  banner:
    location: classpath:grub-banner.txt
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD}

